version: '3.8'

services:

  frontend:
    image: isaofviltrum/front-eval2:latest
    container_name: frontend_app
    restart: always
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - BACKEND_URL=http://10.0.7.39:3000
      - SECRET_KEY=clave_secreta_segura
      - DEBUG=False
    networks:
      - frontend_network

networks:
  frontend_network:
    driver: bridge